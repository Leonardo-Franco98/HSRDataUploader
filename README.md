Auxiliary project to HSRDataBank

This project is a Node.js file that uses Mongoose to map information (characters, light cones, relics and planar ornament) in a JSON file to a MongoDB database and to save all the images related to these data units to local folders.
